let initialState = {
        friendsData: [
            {id: 2, name: 'Kris'},
            {id: 3, name: 'Anna'},
            {id: 4, name: 'Dmitry'}
        ]
};

const sidebarReducer = (state=initialState, action) => {


    return state;
}

export default sidebarReducer;